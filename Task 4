#include<stdio.h>
#include<ctype.h>
int main()
{
	char ch;
	printf("Enter a character:");
	scanf(" %c", &ch);
	 if(isalpha(ch))
	 {
	 	ch=tolower(ch);
	 		switch(ch){
	 	case 'a':
	 	case 'e':
	 	case 'i':
	 	case 'o':
	 	case 'u':
	 	printf("It is a vowel");
	     break;		
	 			
	 			 default:
	 printf("It is a consonant");
	 	}
	
	 	
	 	
	 	
	 	
	
	 	}else{
		
		printf("Error:entered value is not a letter");
		}
	
	return 0;
}
